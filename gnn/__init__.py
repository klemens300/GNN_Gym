"""GNN surrogate for vacancy migration barriers in BCC high-entropy alloys.

Training pipeline (active learning + ALIGNN-style graph network) that maps a
local atomic environment around a migrating vacancy to its NEB migration
barrier. See README for the full workflow.
"""
